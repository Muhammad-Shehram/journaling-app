Reflekto — Reset your password
================================

Hi<%= @resource.name.present? ? " #{@resource.name}" : "" %>,

We received a request to reset the password for your Reflekto account (<%= @resource.email %>).

Reset your password here (expires in 30 minutes, one-time use):
<%= edit_password_url(@resource, reset_password_token: @token) %>

If you didn't request this, ignore this email — your password won't change.

— The Reflekto team
https://reflektoapp.com
